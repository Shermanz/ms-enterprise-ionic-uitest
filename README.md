# IONIC project using tabs template and showing how can you write UI tests using Xamarin.UI.Test
## This project was created using Visual Studio 2015. The solution is inside the folder `ms-enterprise`